#include<stdio.h>
void main() 
{
int x,y,sum=0,num;
 
  printf(" Enter How many Number you want");
  scanf("%d",&y);
 
  printf(" Enter the elements one by one");
  for(i=0;i<y;++i)
   {
     scanf("%d",&num);
     Sum = Sum +num;
 
  return 0;
}
